import 'package:flutter/material.dart';

class RewardsScreen extends StatelessWidget {
  static const String id = "rewards_screen";
  const RewardsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}